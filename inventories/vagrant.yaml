
all:
  hosts:
    nfs:
      ansible_host: 10.0.0.10

    admin:
      ansible_host: 10.0.0.11

    gateway:
      ansible_host: 10.0.0.12

    wfs:
      ansible_host: 10.0.0.13

    wms:
      ansible_host: 10.0.0.14

    wcs:
      ansible_host: 10.0.0.15

    rest:
      ansible_host: 10.0.0.16

    webui:
      ansible_host: 10.0.0.17

    gwc:
      ansible_host: 10.0.0.18

  children:
    java:
      vars:
        logs_basedir: /var/log/gscloud
      hosts:
        admin:
          service:
            name: admin
            jar_file_path: /opt/gscloud/admin.jar
            from_jar: /vagrant/src/apps/infrastructure/admin/target/gs-cloud-admin-service-1.0-SNAPSHOT-bin.jar

        gateway:
        wfs:
        wms:
        wcs:
        rest:
        webui:
        gwc:
    application:
      hosts:
        vagrant:
